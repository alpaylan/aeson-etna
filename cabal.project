-- Workload-narrowed cabal.project: we only test text-iso8601 (the
-- sub-package containing parseQuarterOfYear). The full aeson monorepo
-- has many other packages (attoparsec-aeson, etc.) we don't need.
packages:
    text-iso8601/
    etna/
